import './bootstrap';

/* import Datepicker from 'flowbite-datepicker/Datepicker';

const datepickerEl = document.getElementById('datepicker');
if (datepickerEl) {
  new Datepicker(datepickerEl, {
    autohide: true,
    format:"dd/mm/yyyy",
    language: 'pt-BR',
  });
}


const datepickerElRsvp = document.getElementById('datepicker_rsvp');
if (datepickerElRsvp) {
  new Datepicker(datepickerElRsvp, {
    autohide: true,
    format:"dd/mm/yyyy"
  });
} */